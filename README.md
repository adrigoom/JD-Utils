# JD-Utils

A WIP collection of scripts for modifying or converting several file formats used in UbiArt Framework-based Just Dance games.

## Supported actions

- Converting Cyrillic .ktape.ckd files to romanized lyrics
- Reorganizing KaraokeClip JSON objects in a .ktape.ckd file by order of appearance (useful for modifying or desensitizing song lyrics)

## Installation
*(WARNING: Only Windows is supported for now; I’ll work on making it more cross-platform in the future.)*

(Optional: I recommend creating a virtual environment before installing any packages.)
```bash
python -m venv .venv
```
To install the required packages, run:
```bash
pip install -r requirements.txt
```
Alternatively, you can install them manually:
```bash
pip install transliterate
```
## Usage

Run main.py and follow the on-screen instructions.

## Disclaimer

If you encounter any issue with this tool, please let me know on Discord: @adrigoom


